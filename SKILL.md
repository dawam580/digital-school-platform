---
name: libyan-school-platform
description: نظام ومنصة المدرسة الرقمية الليبية - إدارة الطلاب، الكنترول، الدرجات، الحضور والغياب، الأخصائي الاجتماعي، وتوليد الشهادات الرسمية وفق معايير وزارة التربية والتعليم الليبية.
---

# منصة المدرسة الرقمية (Digital School Platform)

دليل وإرشادات المهارة البرمجية لإدارة وتطوير منظومة مدرسة التعليم الأساسي والثانوي في ليبيا.

---

## 🏛️ السياق المؤسسي وبيانات المدرسة (School Context)

- **المؤسسة المعتمدة:** مدرسة الشهيد امحمد الباعور للتعليم الأساسي (بلدية توكرة - كود: 30713).
- **العام الدراسي:** 1447-1448 هـ الموافق 2025-2026 م.
- **قاعدة بيانات الطلاب:** 873 طالباً حقيقياً موزعين على 33 فصلاً وشعبة دراسية (من 1/1 إلى 9/4).
- **اللائحة الدراسية:** لائحة تنظيم شؤون التعليم والامتحانات الليبية:
  - أعمال السنة: 40 درجة (فترة أولى 20 + فترة ثانية 20).
  - امتحان نهاية الفصل: 60 درجة.
  - المجموع الكلي: 100 درجة (درجة النجاح الصغرى 50 لكل مادة).

---

## 🗄️ معمارية قواعد البيانات والتخزين (Database Architecture)

1. **محرك التخزين المزدوج (Dual-Layer Offline-First Storage):**
   - **IndexedDB (`IndexedDBManager` - Version 3):** المخزن الرئيسي للمعاملات الدفعية وسجلات الطلاب الكبيرة، مع دعم العمل دون اتصال بنسبة 100%.
   - **LocalStorage:** مرآة متزامنة مشفرة بـ `CryptoVaultService` لسرعة الإقلاع ودعم جلسات العمل الفورية.
2. **نمط المستودعات النمطية (`src/services/repositories/`):**
   - `studentRepository`: إدارة سجلات الطلاب والمعدلات.
   - `classRepository`: إدارة الفصول والإحصائيات.
   - `teacherRepository`: حسابات المعلمين وأنصبتهم.
   - `notificationRepository`: التنبيهات والتعاميم.
   - `counselingRepository`: دراسات الحالة الإرشادية.
   - `financeRepository`: الإيرادات والمصروفات والأقساط.
3. **تزامن إحصائيات الفصول اللحظي (`classAnalytics.ts`):**
   - أي تعديل على حضور أو نقل أي طالب يحدّث تلقائياً عدادات الحاضرين والغياب في الفصل (`computeLiveClassStats`).

---

## 🛡️ الأمان والتحكم بالصلاحيات (Security & RBAC)

- **Firebase Auth & Custom Claims:**
  - الدور (`role`) ومعرّف المدرسة (`schoolId`) يُحفظان في JWT Token موقّع مشفراً من خوادم Firebase.
  - عزل كامل للبيانات تحت المسار `/schools/{schoolId}/**` في قواعد `firestore.rules`.
  - عزل تام ومطلق عن أي مشاريع خارجية (مثل مشروع `dawa-ba057`).
- **المعايير المعتمدة:**
  - تعقيم وحماية كافة المدخلات من نصوص XSS عبر `SecurityEngine.sanitizeString`.
  - سجل تدقيق إداري دقيق لكافة العمليات الحساسة عبر `auditLogger.log`.

---

## ⚡ أوامر العمل والتحقق (Standard Commands)

- **تشغيل الاختبارات الآلية (203 اختبارات):**
  ```powershell
  npm.cmd test
  ```
- **فحص الأنواع وبناء الإنتاج:**
  ```powershell
  npx.cmd tsc --noEmit
  npm.cmd run build
  ```
- **مزامنة شجرة وهندسة المشروع (إلزامي بعد أي تعديل كود):**
  ```powershell
  npm.cmd run graph:sync
  ```
